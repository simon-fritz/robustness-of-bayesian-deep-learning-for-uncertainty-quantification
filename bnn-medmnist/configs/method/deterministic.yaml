# Deterministic (non-Bayesian) baseline: standard MAP training, softmax outputs.
name: deterministic
bayesian_layers: []        # no stochastic layers
n_samples: 1               # single forward pass at predict time
optimizer: adam
lr: 1e-3
weight_decay: 5e-4
epochs: 50
