# Experiment: Last-Layer Laplace on PneumoniaMNIST.
defaults:
  - /data: pneumoniamnist
  - /model: small_cnn
  - /method: last_layer_laplace
  - _self_

experiment_name: pneumonia_lll
run_name: pneumonia_lll
seed: 42

# OOD sources to evaluate against (resolved by data/ood_pairs.py — next step).
ood_datasets: [bloodmnist, pathmnist]

output_dir: outputs
checkpoint_dir: checkpoints
